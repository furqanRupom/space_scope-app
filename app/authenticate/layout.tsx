import React from 'react';
import "../globals.css";

const AuthenticateLayout = ({children}:any) => {
    return (
        <div>
            {children}
        </div>
    );
};

export default AuthenticateLayout;
